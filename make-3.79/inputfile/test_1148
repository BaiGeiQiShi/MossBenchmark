#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir0 ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -f $INDIR/n10test/makefile-1a -k -n -s > $OUTDIR/o1148 2>&1
$INDIR/util/makeoutputcleanup $OUTDIR/o1148
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi

sed -i -n '$d' $OUTDIR/o1148
cat hello >> $OUTDIR/o1148
cd ..
rm -r -f sdir
