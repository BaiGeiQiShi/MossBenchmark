#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir0 ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -d -f $INDIR/n10test/makefile-1a -i -n -p -s -w > $OUTDIR/o858 2>&1
$INDIR/util/rm-makestuff -a $OUTDIR/o858
$INDIR/util/makeoutputcleanup $OUTDIR/o858
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi
sed -i -n '$d' $OUTDIR/o858

cat hello >> $OUTDIR/o858
cd ..
rm -r -f sdir
