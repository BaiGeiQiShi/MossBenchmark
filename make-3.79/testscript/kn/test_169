#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir2 ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -f $INDIR/n10test/makefile-2a -i -n -p -s > $OUTDIR/o169 2>&1
$INDIR/util/rm-makestuff -p $OUTDIR/o169
$INDIR/util/makeoutputcleanup $OUTDIR/o169
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi
sed -i -n '$d' $OUTDIR/o169

cat hello >> $OUTDIR/o169
cd ..
rm -r -f sdir
