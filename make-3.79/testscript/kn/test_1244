#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir-cov16l ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -p > $OUTDIR/o1244 2>&1
$INDIR/util/rm-makestuff -p $OUTDIR/o1244
$INDIR/util/makeoutputcleanup $OUTDIR/o1244
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi
sed -i -n '$d' $OUTDIR/o1244

cat make >> $OUTDIR/o1244
cd ..
rm -r -f sdir
