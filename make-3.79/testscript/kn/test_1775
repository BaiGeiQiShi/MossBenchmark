#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir2 ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -f $INDIR/n10test/makefile-2a -n -p -w > $OUTDIR/o1775 2>&1
$INDIR/util/rm-makestuff -p $OUTDIR/o1775
$INDIR/util/makeoutputcleanup $OUTDIR/o1775
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi
sed -i -n '$d' $OUTDIR/o1775

cat hello >> $OUTDIR/o1775
cd ..
rm -r -f sdir
