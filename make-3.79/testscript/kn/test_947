#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir-cov1 ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -- > $OUTDIR/o947 2>&1
$INDIR/util/makeoutputcleanup $OUTDIR/o947
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi

sed -i -n '$d' $OUTDIR/o947
cd ..
rm -r -f sdir
