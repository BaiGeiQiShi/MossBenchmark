#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir2 ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -d -e -f $INDIR/n10test/makefile-1a -p -w > $OUTDIR/o1277 2>&1
$INDIR/util/rm-makestuff -a $OUTDIR/o1277
$INDIR/util/makeoutputcleanup $OUTDIR/o1277
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi
sed -i -n '$d' $OUTDIR/o1277

cat hello >> $OUTDIR/o1277
cd ..
rm -r -f sdir
