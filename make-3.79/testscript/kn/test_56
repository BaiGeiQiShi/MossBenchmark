#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir2 ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -e -f $INDIR/n10test/makefile-2a -i -n -p -s > $OUTDIR/o56 2>&1
$INDIR/util/rm-makestuff -p $OUTDIR/o56
$INDIR/util/makeoutputcleanup $OUTDIR/o56
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi
sed -i -n '$d' $OUTDIR/o56

cat hello >> $OUTDIR/o56
cd ..
rm -r -f sdir
