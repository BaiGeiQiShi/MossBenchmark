#!/bin/bash

BIN=$1
OUTDIR=$2
TIMEOUT=$3
INDIR=$4
CURRDIR=$(pwd)

cp -r -p $INDIR/n10test/sdir-cov16g ./sdir
cd sdir
timeout -k 9 ${TIMEOUT}s $BIN -p > $OUTDIR/o482 2>&1
$INDIR/util/rm-makestuff -p $OUTDIR/o482
$INDIR/util/makeoutputcleanup $OUTDIR/o482
if [ -f default.profraw ]; then
    mv default.profraw ${CURRDIR}/
fi
sed -i -n '$d' $OUTDIR/o482

cat make >> $OUTDIR/o482
cd ..
rm -r -f sdir
